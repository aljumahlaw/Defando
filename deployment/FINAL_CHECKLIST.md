# ✅ قائمة التحقق النهائية - نظام إدارة المستندات القانونية

استخدم هذه القائمة للتأكد من إعداد النظام بشكل صحيح قبل التشغيل.

---

## 📋 قبل البدء

- [ ] قرأت `docs/README.md` وفهمت بنية المشروع
- [ ] قرأت `docs/DELIVERY_NOTE.md` لفهم محتويات الحزمة
- [ ] لدي صلاحيات مسؤول على الجهاز
- [ ] لدي اتصال بالإنترنت (لتحميل البرامج)

---

## 1️⃣ تثبيت البرامج الأساسية

### .NET 8 SDK
- [ ] تم تثبيت .NET 8 SDK
- [ ] التحقق: `dotnet --version` يعرض `8.0.xxx`
- [ ] **مشكلة:** إذا لم يعمل، راجع: https://dotnet.microsoft.com/download

### PostgreSQL 14+
- [ ] تم تثبيت PostgreSQL 14 أو 16
- [ ] تم حفظ كلمة مرور المستخدم `postgres`
- [ ] تم تشغيل خدمة PostgreSQL
- [ ] التحقق: `psql --version` يعمل
- [ ] **مشكلة:** إذا لم يعمل، راجع `deployment/DEPLOYMENT_GUIDE.md`

### Visual Studio 2022 أو VS Code
- [ ] تم تثبيت Visual Studio 2022 أو VS Code
- [ ] تم تثبيت C# extension (لـ VS Code)
- [ ] **مشكلة:** راجع `deployment/DEPLOYMENT_GUIDE.md`

### Tesseract OCR (اختياري - للـ OCR)
- [ ] تم تثبيت Tesseract OCR 5.x
- [ ] تم تحميل ملفات اللغة العربية (`ara.traineddata`)
- [ ] التحقق: `tesseract --list-langs` يعرض `ara`
- [ ] **مشكلة:** راجع `docs/OCR_SETUP.md`

---

## 2️⃣ إعداد قاعدة البيانات

### إنشاء قاعدة البيانات
- [ ] فتح pgAdmin 4 أو psql
- [ ] إنشاء قاعدة بيانات: `CREATE DATABASE legal_doc_system;`
- [ ] إنشاء مستخدم: `CREATE USER doc_user WITH PASSWORD 'your_password';`
- [ ] منح الصلاحيات: `GRANT ALL PRIVILEGES ON DATABASE legal_doc_system TO doc_user;`

### تنفيذ Schema
- [ ] فتح `database/schema.sql`
- [ ] تنفيذ السكربت في قاعدة البيانات
- [ ] التحقق من الجداول: `\dt` (يجب أن ترى 14 جدول)
- [ ] **مشكلة:** إذا فشل، تحقق من صلاحيات المستخدم

### التحقق من Extensions
- [ ] التحقق من `pgcrypto` extension
- [ ] التحقق من `uuid-ossp` extension
- [ ] **مشكلة:** إذا لم تكن موجودة، شغّل: `CREATE EXTENSION IF NOT EXISTS "pgcrypto";`

---

## 3️⃣ إعداد المشروع

### استعادة NuGet Packages
- [ ] فتح Terminal في مجلد `src/`
- [ ] تشغيل: `dotnet restore`
- [ ] التحقق من عدم وجود أخطاء
- [ ] **مشكلة:** إذا فشل، تحقق من اتصال الإنترنت

### تثبيت Packages المطلوبة
- [ ] `dotnet add package Npgsql.EntityFrameworkCore.PostgreSQL`
- [ ] `dotnet add package Hangfire.Core`
- [ ] `dotnet add package Hangfire.AspNetCore`
- [ ] `dotnet add package Hangfire.PostgreSql`
- [ ] `dotnet add package BCrypt.Net-Next`
- [ ] **مشكلة:** إذا فشل، تحقق من اتصال الإنترنت

### تحديث appsettings.json
- [ ] تحديث `ConnectionStrings:DefaultConnection`
- [ ] تحديث `FileStorage:BasePath` (إنشاء المجلد إذا لزم)
- [ ] تحديث `Ocr:TesseractPath` (إذا كان OCR مفعّل)
- [ ] **مشكلة:** تحقق من صحة المسارات

---

## 4️⃣ إعداد File Storage

### التخزين المحلي
- [ ] إنشاء مجلد: `D:\LegalDMS\Files` (أو المسار المحدد)
- [ ] التحقق من الصلاحيات (Read/Write)
- [ ] **مشكلة:** إذا لم يعمل، تحقق من الصلاحيات

### NAS Storage (اختياري)
- [ ] ربط NAS كمجلد شبكي
- [ ] تحديث `FileStorage:BasePath` في `appsettings.json`
- [ ] التحقق من الاتصال
- [ ] **مشكلة:** راجع `docs/FILE_STORAGE_GUIDE.md`

---

## 5️⃣ تشغيل التطبيق

### التحقق من الكود
- [ ] فتح المشروع في Visual Studio أو VS Code
- [ ] التحقق من عدم وجود أخطاء في:
  - `src/Program.cs`
  - `src/Data/ApplicationDbContext.cs`
  - `src/Services/*.cs`
  - `src/Controllers/*.cs`
- [ ] **مشكلة:** راجع Logs في IDE

### تشغيل التطبيق
- [ ] من مجلد `src/`: `dotnet run`
- [ ] التحقق من عدم وجود أخطاء في Console
- [ ] التحقق من رسالة: "Now listening on: https://localhost:5001"
- [ ] **مشكلة:** راجع قسم "استكشاف الأخطاء" أدناه

---

## 6️⃣ اختبار الوظائف

### اختبار قاعدة البيانات
- [ ] فتح المتصفح: `https://localhost:5001`
- [ ] التحقق من عدم وجود أخطاء Database connection
- [ ] **مشكلة:** راجع Connection String

### اختبار Authentication
- [ ] فتح صفحة Login: `/login`
- [ ] محاولة تسجيل دخول (يحتاج مستخدم في قاعدة البيانات)
- [ ] **مشكلة:** إنشاء مستخدم أولي في قاعدة البيانات

### اختبار الصفحات
- [ ] صفحة Dashboard: `/`
- [ ] صفحة المستندات: `/documents`
- [ ] صفحة المجلدات: `/folders`
- [ ] صفحة المهام: `/tasks`
- [ ] **مشكلة:** راجع Logs في Console

### اختبار API
- [ ] فتح Postman أو Swagger
- [ ] اختبار: `GET /api/documents`
- [ ] اختبار: `GET /api/folders`
- [ ] **مشكلة:** راجع Authorization headers

### اختبار Hangfire
- [ ] فتح: `https://localhost:5001/hangfire`
- [ ] التحقق من Dashboard
- [ ] التحقق من Recurring Jobs
- [ ] **مشكلة:** راجع `docs/HANGFIRE_GUIDE.md`

---

## 7️⃣ إعدادات إضافية

### إنشاء مستخدم أولي
- [ ] إنشاء مستخدم Admin في قاعدة البيانات
- [ ] تشفير كلمة المرور باستخدام BCrypt
- [ ] **مشكلة:** راجع `UserService.CreateUserAsync()`

### إعداد SMTP (للإشعارات)
- [ ] تحديث `appsettings.json` بإعدادات SMTP
- [ ] اختبار إرسال بريد إلكتروني
- [ ] **مشكلة:** راجع إعدادات البريد الإلكتروني

### إعداد النسخ الاحتياطي
- [ ] إعداد Backup strategy
- [ ] اختبار Backup process
- [ ] **مشكلة:** راجع `docs/FILE_STORAGE_GUIDE.md`

---

## 🚨 استكشاف الأخطاء الشائعة

### مشكلة: "Connection string not found"
**الحل:**
1. تحقق من وجود `appsettings.json` في `src/`
2. تحقق من صحة Connection String
3. تحقق من وجود قاعدة البيانات

### مشكلة: "Table does not exist"
**الحل:**
1. تأكد من تنفيذ `database/schema.sql`
2. تحقق من اسم قاعدة البيانات
3. تحقق من صلاحيات المستخدم

### مشكلة: "Port 5001 already in use"
**الحل:**
1. أغلق التطبيق الذي يستخدم البورت
2. أو غيّر البورت في `appsettings.json`:
   ```json
   "Kestrel": {
     "Endpoints": {
       "Http": {
         "Url": "http://localhost:5002"
       }
     }
   }
   ```

### مشكلة: "Tesseract executable not found"
**الحل:**
1. تحقق من المسار في `appsettings.json`
2. تأكد من تثبيت Tesseract
3. راجع `docs/OCR_SETUP.md`

### مشكلة: "File not found" (File Storage)
**الحل:**
1. تحقق من `FileStorage:BasePath` في `appsettings.json`
2. تأكد من وجود المجلد
3. تحقق من الصلاحيات

### مشكلة: "Hangfire Dashboard not accessible"
**الحل:**
1. تحقق من تثبيت Hangfire packages
2. تحقق من Connection String
3. راجع `docs/HANGFIRE_GUIDE.md`

---

## 📞 الدعم الفني

### الوثائق المرجعية
- `docs/README.md` - نظرة عامة
- `docs/PROJECT_STRUCTURE.md` - هيكل المشروع
- `docs/DECISIONS.md` - القرارات التقنية
- `deployment/DEPLOYMENT_GUIDE.md` - دليل النشر
- `docs/OCR_SETUP.md` - إعداد OCR
- `docs/FILE_STORAGE_GUIDE.md` - دليل التخزين
- `docs/HANGFIRE_GUIDE.md` - دليل Hangfire

### قنوات الدعم
- راجع Logs في Console
- راجع Database logs
- راجع Application logs

---

## ✅ بعد إكمال القائمة

إذا أكملت جميع النقاط أعلاه:

- [ ] ✅ النظام يعمل بشكل صحيح
- [ ] ✅ قاعدة البيانات متصلة
- [ ] ✅ جميع Services تعمل
- [ ] ✅ الواجهة تظهر بدون أخطاء
- [ ] ✅ API يعمل
- [ ] ✅ Hangfire يعمل

**🎉 مبروك! النظام جاهز للاستخدام والتطوير!**

---

## 📝 ملاحظات إضافية

- احتفظ بنسخة احتياطية من `appsettings.json`
- سجّل جميع التغييرات في Configuration
- راجع Logs بانتظام
- اختبر النسخ الاحتياطي دورياً

---

**آخر تحديث:** 2025  
**الإصدار:** 1.0.0

