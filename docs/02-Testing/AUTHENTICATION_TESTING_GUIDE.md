# دليل اختبار نظام Authentication
## Authentication Testing Guide

**التاريخ:** 2025  
**الإصدار:** 1.0  
**الجمهور المستهدف:** المختبرون غير التقنيين

---

## 📋 محتويات الدليل

1. [متطلبات الاختبار](#1-متطلبات-الاختبار)
2. [سيناريوهات الاختبار](#2-سيناريوهات-الاختبار)
3. [Checklist سريعة](#3-checklist-سريعة)

---

## 1. متطلبات الاختبار

### 1.1 المستخدمون المطلوبون

قبل البدء بالاختبار، يجب أن يكون لديك **مستخدمين** في قاعدة البيانات:

#### ✅ **المستخدم 1: مستخدم عادي**
- **اسم المستخدم:** `user1`
- **كلمة المرور:** (يجب أن تكون معروفة لديك)
- **الدور (Role):** `junior_lawyer` أو `legal_researcher` (أي دور غير `admin`)

#### ✅ **المستخدم 2: مدير (Admin)**
- **اسم المستخدم:** `admin`
- **كلمة المرور:** (يجب أن تكون معروفة لديك)
- **الدور (Role):** `admin`

---

### 1.2 التحقق من وجود المستخدمين

**إذا لم يكن لديك مستخدمين:**
1. افتح قاعدة البيانات (PostgreSQL)
2. أدخل البيانات التالية في جدول `users`:

```sql
-- مستخدم عادي
INSERT INTO users (username, password_hash, full_name, email, role, is_active, created_at)
VALUES ('user1', '$2a$11$...', 'مستخدم تجريبي', 'user1@test.com', 'junior_lawyer', true, NOW());

-- مستخدم Admin
INSERT INTO users (username, password_hash, full_name, email, role, is_active, created_at)
VALUES ('admin', '$2a$11$...', 'مدير النظام', 'admin@test.com', 'admin', true, NOW());
```

**ملاحظة:** يجب تشفير كلمات المرور باستخدام BCrypt قبل الإدخال.

---

## 2. سيناريوهات الاختبار

---

### Test Case 1: Login/Logout Flow
**الهدف:** التحقق من أن تسجيل الدخول والخروج يعملان بشكل صحيح.

#### 📝 **الخطوات:**

1. **افتح المتصفح** واذهب إلى: `https://localhost:5001/login`
   - **ماذا تتوقع:** يجب أن ترى صفحة تسجيل الدخول مع حقول "اسم المستخدم" و "كلمة المرور"

2. **أدخل بيانات خاطئة:**
   - اسم المستخدم: `wronguser`
   - كلمة المرور: `wrongpass`
   - اضغط "تسجيل الدخول"
   - **ماذا تتوقع:** يجب أن ترى رسالة خطأ: "اسم المستخدم أو كلمة المرور غير صحيحة"

3. **أدخل بيانات صحيحة (user1):**
   - اسم المستخدم: `user1`
   - كلمة المرور: (كلمة المرور الصحيحة)
   - اضغط "تسجيل الدخول"
   - **ماذا تتوقع:** 
     - يجب أن يتم التحويل تلقائياً إلى الصفحة الرئيسية (`/`)
     - يجب أن ترى في أعلى الصفحة: "مرحباً، [اسم المستخدم]"
     - يجب أن ترى زر "تسجيل خروج"

4. **تحقق من تسجيل الدخول:**
   - افتح صفحة `/documents`
   - **ماذا تتوقع:** يجب أن ترى قائمة المستندات (أو رسالة "لا توجد مستندات")

5. **تسجيل الخروج:**
   - اضغط على زر "تسجيل خروج" في أعلى الصفحة
   - **ماذا تتوقع:** 
     - يجب أن يتم التحويل تلقائياً إلى صفحة `/login`
     - يجب أن تختفي رسالة "مرحباً، [اسم المستخدم]"

6. **محاولة الوصول بعد تسجيل الخروج:**
   - حاول فتح `/documents` مباشرة في المتصفح
   - **ماذا تتوقع:** يجب أن يتم التحويل تلقائياً إلى `/login`

#### ✅ **النتيجة المتوقعة:**
- ✅ تسجيل الدخول يعمل
- ✅ التحويل بعد تسجيل الدخول يعمل
- ✅ تسجيل الخروج يعمل
- ✅ التحويل بعد تسجيل الخروج يعمل

#### 🔍 **كيفية التحقق:**
- تحقق من ظهور اسم المستخدم في أعلى الصفحة بعد تسجيل الدخول
- تحقق من التحويل التلقائي للصفحات
- تحقق من اختفاء اسم المستخدم بعد تسجيل الخروج

#### ❌ **ماذا تفعل في حالة الفشل:**
- تحقق من أن كلمة المرور صحيحة
- تحقق من أن المستخدم `is_active = true` في قاعدة البيانات
- تحقق من Console في المتصفح (F12) للأخطاء
- تحقق من Logs في التطبيق

---

### Test Case 2: [Authorize] Attributes
**الهدف:** التحقق من أن الصفحات المحمية تطلب تسجيل الدخول.

#### 📝 **الخطوات:**

1. **افتح المتصفح** بدون تسجيل دخول

2. **حاول الوصول لصفحة محمية:**
   - اذهب مباشرة إلى: `https://localhost:5001/documents`
   - **ماذا تتوقع:** 
     - يجب أن يتم التحويل تلقائياً إلى `/login`
     - يجب أن ترى صفحة تسجيل الدخول

3. **سجّل دخول:**
   - أدخل بيانات `user1`
   - اضغط "تسجيل الدخول"
   - **ماذا تتوقع:** يجب أن يتم التحويل إلى `/` (الصفحة الرئيسية)

4. **الوصول للصفحة المحمية:**
   - اذهب إلى: `https://localhost:5001/documents`
   - **ماذا تتوقع:** يجب أن ترى صفحة المستندات (أو رسالة "لا توجد مستندات")

5. **اختبر صفحات أخرى محمية:**
   - `/folders` - يجب أن تعمل
   - `/tasks` - يجب أن تعمل
   - `/search` - يجب أن تعمل
   - `/settings` - يجب أن تعمل

#### ✅ **النتيجة المتوقعة:**
- ✅ جميع الصفحات المحمية تطلب تسجيل الدخول
- ✅ التحويل التلقائي إلى `/login` يعمل
- ✅ بعد تسجيل الدخول، يمكن الوصول للصفحات المحمية

#### 🔍 **كيفية التحقق:**
- حاول الوصول لصفحة محمية بدون تسجيل دخول → يجب أن تذهب إلى `/login`
- بعد تسجيل الدخول، حاول الوصول لنفس الصفحة → يجب أن تعمل

#### ❌ **ماذا تفعل في حالة الفشل:**
- تحقق من أن `[Authorize]` موجود في بداية الصفحة
- تحقق من أن `UseAuthentication()` و `UseAuthorization()` موجودان في `Program.cs`
- تحقق من Console في المتصفح (F12) للأخطاء

---

### Test Case 3: Role-based Authorization
**الهدف:** التحقق من أن صفحات Admin محمية ولا يمكن للمستخدمين العاديين الوصول إليها.

#### 📝 **الخطوات:**

1. **سجّل دخول كمستخدم عادي (user1):**
   - اذهب إلى `/login`
   - أدخل بيانات `user1`
   - اضغط "تسجيل الدخول"

2. **حاول الوصول لصفحة Admin:**
   - اذهب مباشرة إلى: `https://localhost:5001/users`
   - **ماذا تتوقع:** 
     - يجب أن يتم التحويل تلقائياً إلى `/login`
     - أو يجب أن ترى رسالة "Access Denied" أو "ليس لديك صلاحية"

3. **اختبر صفحات Admin أخرى:**
   - `/settings/smtp` - يجب أن يتم التحويل إلى `/login`
   - `/shared-links` - يجب أن يتم التحويل إلى `/login`

4. **سجّل خروج:**
   - اضغط "تسجيل خروج"

5. **سجّل دخول كـ Admin:**
   - اذهب إلى `/login`
   - أدخل بيانات `admin`
   - اضغط "تسجيل الدخول"

6. **الوصول لصفحات Admin:**
   - اذهب إلى: `https://localhost:5001/users`
   - **ماذا تتوقع:** يجب أن ترى صفحة إدارة المستخدمين

7. **اختبر صفحات Admin أخرى:**
   - `/settings/smtp` - يجب أن تعمل
   - `/shared-links` - يجب أن تعمل

#### ✅ **النتيجة المتوقعة:**
- ✅ المستخدمون العاديون لا يمكنهم الوصول لصفحات Admin
- ✅ المديرون (admin) يمكنهم الوصول لصفحات Admin
- ✅ التحويل التلقائي يعمل للمستخدمين غير المصرح لهم

#### 🔍 **كيفية التحقق:**
- سجّل دخول كمستخدم عادي → حاول الوصول لـ `/users` → يجب أن يتم التحويل
- سجّل دخول كـ admin → حاول الوصول لـ `/users` → يجب أن تعمل

#### ❌ **ماذا تفعل في حالة الفشل:**
- تحقق من أن `[Authorize(Roles = "admin")]` موجود في بداية الصفحة
- تحقق من أن دور المستخدم في قاعدة البيانات هو `admin` (وليس `Admin` أو `ADMIN`)
- تحقق من أن Claims تحتوي على Role في `AuthService.LoginAsync`

---

### Test Case 4: Sliding Expiration (اختياري)
**الهدف:** التحقق من أن الجلسة تمتد تلقائياً عند النشاط.

#### 📝 **الخطوات:**

1. **سجّل دخول:**
   - اذهب إلى `/login`
   - أدخل بيانات `user1`
   - اضغط "تسجيل الدخول"

2. **انتظر 25 دقيقة:**
   - **ملاحظة:** هذا اختبار طويل، يمكنك تخطيه في الاختبار الأولي
   - اترك المتصفح مفتوحاً بدون أي نشاط لمدة 25 دقيقة

3. **قم بنشاط:**
   - بعد 25 دقيقة، افتح صفحة `/documents`
   - **ماذا تتوقع:** يجب أن تعمل الصفحة (الجلسة تم تمديدها)

4. **انتظر 30 دقيقة بدون نشاط:**
   - اترك المتصفح مفتوحاً بدون أي نشاط لمدة 30 دقيقة

5. **حاول الوصول لصفحة محمية:**
   - افتح صفحة `/documents`
   - **ماذا تتوقع:** 
     - يجب أن يتم التحويل إلى `/login`
     - (لأن الجلسة انتهت بعد 30 دقيقة بدون نشاط)

#### ✅ **النتيجة المتوقعة:**
- ✅ الجلسة تمتد تلقائياً عند النشاط (Sliding Expiration)
- ✅ الجلسة تنتهي بعد 30 دقيقة بدون نشاط

#### 🔍 **كيفية التحقق:**
- بعد 25 دقيقة من النشاط، يجب أن تعمل الصفحات
- بعد 30 دقيقة بدون نشاط، يجب أن يتم التحويل إلى `/login`

#### ❌ **ماذا تفعل في حالة الفشل:**
- تحقق من أن `SlidingExpiration = true` في `Program.cs`
- تحقق من أن `ExpireTimeSpan = TimeSpan.FromMinutes(30)` في `Program.cs`
- تحقق من أن Session Timeout هو 30 دقيقة

---

## 3. Checklist سريعة

استخدم هذه القائمة للتحقق السريع من جميع الوظائف:

### ✅ **Authentication الأساسي:**
- [ ] Login يعمل (يمكن تسجيل الدخول)
- [ ] Logout يعمل (يمكن تسجيل الخروج)
- [ ] التحويل إلى `/login` يعمل (عند محاولة الوصول لصفحة محمية بدون تسجيل دخول)

### ✅ **Authorization:**
- [ ] `[Authorize]` يعمل (الصفحات المحمية تطلب تسجيل الدخول)
- [ ] `[Authorize(Roles = "admin")]` يعمل (صفحات Admin محمية)
- [ ] المستخدمون العاديون لا يمكنهم الوصول لصفحات Admin
- [ ] المديرون (admin) يمكنهم الوصول لصفحات Admin

### ✅ **الصفحات المفتوحة:**
- [ ] `SharedDocument.razor` مفتوح بدون تسجيل دخول (رابط عام)
- [ ] `Error.razor` مفتوح بدون تسجيل دخول
- [ ] `Login.razor` مفتوح بدون تسجيل دخول

### ✅ **الوظائف الإضافية:**
- [ ] اسم المستخدم يظهر في `MainLayout` بعد تسجيل الدخول
- [ ] زر "تسجيل خروج" يظهر بعد تسجيل الدخول
- [ ] رسائل الخطأ تظهر عند إدخال بيانات خاطئة

---

## 4. صفحات الاختبار

### 4.1 الصفحات المحمية (يجب أن تطلب تسجيل دخول):

| الصفحة | المسار | نوع الحماية |
|--------|--------|-------------|
| لوحة التحكم | `/` | `[Authorize]` |
| المستندات | `/documents` | `[Authorize]` |
| تفاصيل المستند | `/documents/{id}` | `[Authorize]` |
| إصدارات المستند | `/documents/{id}/versions` | `[Authorize]` |
| المجلدات | `/folders` | `[Authorize]` |
| المهام | `/tasks` | `[Authorize]` |
| البحث | `/search` | `[Authorize]` |
| الإعدادات | `/settings` | `[Authorize]` |
| الصادر | `/outgoing` | `[Authorize]` |
| الوارد | `/incoming` | `[Authorize]` |
| إنشاء رابط مشاركة | `/documents/{id}/share` | `[Authorize]` |

### 4.2 صفحات Admin (يجب أن تطلب دور admin):

| الصفحة | المسار | نوع الحماية |
|--------|--------|-------------|
| إدارة المستخدمين | `/users` | `[Authorize(Roles = "admin")]` |
| إعدادات SMTP | `/settings/smtp` | `[Authorize(Roles = "admin")]` |
| إدارة الروابط المشتركة | `/shared-links` | `[Authorize(Roles = "admin")]` |

### 4.3 الصفحات المفتوحة (لا تحتاج تسجيل دخول):

| الصفحة | المسار | السبب |
|--------|--------|-------|
| تسجيل الدخول | `/login` | صفحة تسجيل الدخول |
| صفحة الخطأ | `/error` | صفحة الأخطاء |
| مستند مشترك | `/shared/{token}` | رابط عام للمشاركة |

---

## 5. نصائح للاختبار

### 5.1 استخدام Incognito/Private Mode:
- استخدم وضع التصفح الخاص لاختبار تسجيل الدخول من الصفر
- هذا يضمن أنك تبدأ بدون أي Cookies أو Session

### 5.2 استخدام متصفحات متعددة:
- افتح متصفحين مختلفين
- سجّل دخول كمستخدم عادي في المتصفح الأول
- سجّل دخول كـ admin في المتصفح الثاني
- اختبر الوصول للصفحات في كلا المتصفحين

### 5.3 فحص Console:
- اضغط `F12` في المتصفح لفتح Developer Tools
- اذهب إلى تبويب "Console"
- تحقق من عدم وجود أخطاء JavaScript

### 5.4 فحص Network:
- في Developer Tools، اذهب إلى تبويب "Network"
- راقب الطلبات (Requests) عند تسجيل الدخول
- تحقق من أن Cookie يتم إرساله في الطلبات التالية

---

## 6. المشاكل الشائعة وحلولها

### المشكلة: لا يتم التحويل إلى `/login`
**الحل:**
- تحقق من أن `UseAuthentication()` و `UseAuthorization()` موجودان في `Program.cs`
- تحقق من أن `[Authorize]` موجود في الصفحة

### المشكلة: يمكن للمستخدم العادي الوصول لصفحات Admin
**الحل:**
- تحقق من أن `[Authorize(Roles = "admin")]` موجود في الصفحة
- تحقق من أن دور المستخدم في قاعدة البيانات هو `admin` (بأحرف صغيرة)

### المشكلة: تسجيل الدخول لا يعمل
**الحل:**
- تحقق من أن كلمة المرور صحيحة
- تحقق من أن المستخدم `is_active = true` في قاعدة البيانات
- تحقق من Console في المتصفح للأخطاء

### المشكلة: الجلسة تنتهي بسرعة
**الحل:**
- تحقق من أن `ExpireTimeSpan = TimeSpan.FromMinutes(30)` في `Program.cs`
- تحقق من أن `SlidingExpiration = true` في `Program.cs`

---

## 7. الخلاصة

بعد إكمال جميع الاختبارات، يجب أن تكون قادراً على:

- ✅ تسجيل الدخول والخروج
- الوصول للصفحات المحمية بعد تسجيل الدخول
- منع الوصول للصفحات المحمية بدون تسجيل دخول
- منع المستخدمين العاديين من الوصول لصفحات Admin
- السماح للمديرين بالوصول لصفحات Admin
- الوصول للصفحات المفتوحة بدون تسجيل دخول

---

**آخر تحديث:** 2025  
**الحالة:** ✅ جاهز للاستخدام

